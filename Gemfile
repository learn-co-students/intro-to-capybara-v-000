# This Gemfile is copied fr another lab,
# just so I could get shotgun to work.

source "https://rubygems.org"

# gem "rails"

gem 'sinatra'
    gem 'rake'
    gem 'thin'
    gem 'shotgun'
    gem 'pry'

group :test do
      gem 'rspec'
      gem 'capybara'
      gem 'rack-test'
    end
